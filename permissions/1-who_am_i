import os
import pwd

# Get the effective user ID
euid = os.geteuid()

# Get the username associated with the effective user ID
username = pwd.getpwuid(euid).pw_name

print(f"Effective username: {username}")
