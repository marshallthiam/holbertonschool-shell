#!/bin/bash

# Get the current owner of the file
owner=$(stat -c %U hello)

# Check if the owner is guillaume
if [ "$owner" = "guillaume" ]; then
    sudo chown vincent hello
    echo "Ownership changed to vincent."
else
    echo "File is not owned by guillaume. No changes made."
fi

